console.log("Naimur Rahman D");

// Checking is the number is good or bad

const sum = (num1) => {
  if (num1 > 20 || 200 || 400) {
    return true;
  } else {
    return false;
  }
};

console.log(sum(21));
